{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix React\"",
    "postinstall": "npm install --prefix backend",
    "build": "echo 'React app already compiled and pushed via GitHub. Skipping build.'",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "mongodb": "^7.1.0",
    "next": "^16.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
